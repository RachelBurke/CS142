#include <iostream> //header files
#include <math.h> // ""
using namespace std;

int main () {
	
	double x = 9;
	double y = 3;
	double z, w, r;
	
	// sqrt is the "function" for square root
	cout << sqrt (x) << endl;
	z = sqrt (3);
	
	// pow computes x^y  (in math terms)
	cout << pow(x,y) << endl;
	cout << pow(y,x) << endl; //these two lines are different

	
	
	
	return 0;
}